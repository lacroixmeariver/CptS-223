#ifndef _VECTOR_TEST
#define _VECTOR_TEST
#define MAX_N 957

#include "vectorJosephus.hpp"
#include <fstream>
#include <ctime> 
using std::ifstream; 
using std::ofstream;
using std::stringstream;
class VectorTest
{   
    private:
   //~VectorTest();
    
    public:
    void runSimulation();
};

 #endif