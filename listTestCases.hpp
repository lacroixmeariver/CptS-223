#ifndef _LIST_TEST
#define _LIST_TEST
#define MAX_N 957

#include "listJosephus.hpp"
#include <fstream>
#include <ctime> 
using std::ifstream; 
using std::ofstream;
using std::stringstream;
class ListTest
{   
    private:
   //~ListTest();
    
    public:
    void runSimulation();
};

 #endif